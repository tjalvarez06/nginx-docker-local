# Práctica nginx con Docker Compose

Este proyecto levanta un servidor web nginx en local usando Docker Compose.

## Estructura
- `src/index.html`: página web
- `docker-compose.yml`: configuración del contenedor

## Ejecutar
```bash
docker compose up -d
